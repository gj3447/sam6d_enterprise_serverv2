# Main_Server 프로덕션 환경 설정
HOST=0.0.0.0
PORT=8001
LOG_LEVEL=INFO
RELOAD=false

# 출력 제어 (필요 시 true 로 전환)
# MAIN_SERVER_SAVE_INPUT_IMAGES
#   - false: 입력 RGB/Depth 이미지를 저장하지 않음 (기본)
#   - true : 입력 RGB/Depth 이미지를 output 경로에 저장
MAIN_SERVER_SAVE_INPUT_IMAGES=false
# MAIN_SERVER_SAVE_CAMERA_PARAMS
#   - false: camera_params.json을 생성하지 않음
#   - true : 카메라 파라미터를 camera_params.json으로 저장 (기본)
MAIN_SERVER_SAVE_CAMERA_PARAMS=true
# MAIN_SERVER_SAVE_SERVER_RESPONSES
#   - false: ISM/PEM 서버 응답 JSON을 저장하지 않음 (기본)
#   - true : ism_server_response.json / pem_server_response.json 저장
MAIN_SERVER_SAVE_SERVER_RESPONSES=false

